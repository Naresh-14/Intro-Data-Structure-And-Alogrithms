#include<bits/stdc++.h>
using namespace std;

int main() {
  // 1. simple vector Delaration
  vector<int> v;
  //2. vector initialization with size and default values
  vector<int> v1(5,20);
  //3.vector initialization with size only
  vector<int> v2(5);
  //4.copying one vector to another
  vector<int> v3(v1);
  return 0;
}